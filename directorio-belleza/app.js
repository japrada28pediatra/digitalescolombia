const input=document.querySelector('#search'),city=document.querySelector('#city'),category=document.querySelector('#category'),cards=document.querySelector('#cards'),count=document.querySelector('#result-count');if(input&&window.DIRECTORY_DATA){const data=window.DIRECTORY_DATA;function render(){const q=(input.value||'').toLowerCase().trim(),c=city.value,k=category.value;const out=data.filter(r=>(!q||[r.nombre,r.ciudad,r.categoría,r.servicios].join(' ').toLowerCase().includes(q))&&(!c||r.ciudad===c)&&(!k||r.categoría.replaceAll('-',' ').replace(/\w/g,x=>x.toUpperCase())===k)).slice(0,60);cards.innerHTML=out.map(r=>{const s=r.nombre.toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'')+'-'+r.ciudad.toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g,'').replace(/[^a-z0-9]+/g,'-')+'-'+r.id;return `<article class="card"><span class="tag">${r.categoría}</span><h3><a href="negocios/${s}.html">${r.nombre}</a></h3><p class="muted">${r.ciudad}, ${r.departamento}</p><p>${(r.servicios||'Servicios de belleza y cuidado personal.').slice(0,180)}</p><div class="actions">${r['Google Maps']?`<a class="btn btn-small" href="${r['Google Maps']}" target="_blank">Mapa</a>`:''}${r.WhatsApp?`<a class="btn btn-small btn-green" href="${r.WhatsApp}" target="_blank">WhatsApp</a>`:''}</div></article>`}).join('');count.textContent=`Mostrando ${out.length} de ${data.length}`;}[input,city,category].forEach(x=>x.addEventListener('input',render));render();}